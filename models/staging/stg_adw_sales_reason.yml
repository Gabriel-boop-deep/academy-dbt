version: 2

models:
  - name: stg_adw_sales_reason
    description: A formatted copy of the salesreason table from the sap_adw database.
    columns:
      - name: sales_reason_id
        description: Identifier of the sales reason.
        tests:
          - not_null
      - name: sales_reason_name
        description: Name of the sales reason.
      - name: reason_type
        description: Type of the reason.

